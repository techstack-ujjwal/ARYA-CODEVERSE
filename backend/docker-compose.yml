version: '3.8'

services:
  api:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    container_name: eval_engine_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=development
      - DEBUG=true
    env_file:
      - ../.env
